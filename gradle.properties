org.gradle.jvmargs=-Xmx3G

# Mod version is auto-generated as <major>.<minor>.<patch>
# - In GitHub Actions, <patch> comes from GITHUB_RUN_NUMBER (monotonically increasing)
# - Locally, <patch> falls back to git commit count (or 0 if git isn't available)
mod_version_major=1
mod_version_minor=0
