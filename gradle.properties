org.gradle.jvmargs=-Xmx3G

# Mod version is auto-generated as <major>.<minor>.<commitCount>+g<shortSha>
# - Deterministic per commit (stable across rebuilds in CI)
# - Used in mods.toml via ${file.jarVersion}
mod_version_major=1
mod_version_minor=0
