{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.pebble_wren.main": {
      "initial_state": "default",
      "states": {
        "default": {
          "animations": [
            "animation.pebble_wren.idle"
          ],
          "transitions": [
            { "flying": "!query.is_on_ground" },
            { "walking": "query.is_moving" }
          ]
        },
        "walking": {
          "animations": [
            "animation.pebble_wren.walk"
          ],
          "transitions": [
            { "flying": "!query.is_on_ground" },
            { "default": "!query.is_moving" }
          ]
        },
        "flying": {
          "animations": [
            "animation.pebble_wren.fly"
          ],
          "transitions": [
            { "walking": "query.is_on_ground && query.is_moving" },
            { "default": "query.is_on_ground && !query.is_moving" }
          ]
        }
      }
    }
  }
}
